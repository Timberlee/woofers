source 'https://rubygems.org'

gem 'sinatra'
gem 'activerecord'
gem 'sinatra-activerecord'
gem 'rake'
gem 'faker'
gem 'bcrypt'
#gem 'sinatra-flash' - include in layout head
#gem 'sinatra-reloader'

#Production can't see this.
group :development do
  gem 'sqlite3'
end

#Development can't see this. Although Sinatra can but don't worry about it.
group :production do
  gem 'pg' #po-grey jem
end
